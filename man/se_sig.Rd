% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loopFuns.R
\name{se_sig}
\alias{se_sig}
\title{Calculate One-Sided Confidence Level (\%)}
\usage{
se_sig(mu, vari)
}
\arguments{
\item{mu}{Numeric vector, or SpatRaster of mean (location) values to test against zero.}

\item{vari}{Numeric vector, or SpatRaster of variances corresponding to each \code{mu}.}
}
\value{
A numeric vector of confidence levels (0–100\%), each rounded to one decimal place.
}
\description{
Computes the one-sided confidence level, defined as (1 − p-value) × 100, for testing whether each mean (\code{mu}) differs from zero under a normal approximation.
}
\details{
For each element:
\enumerate{
\item Calculate the standard error:
\code{se = sqrt(vari)}.
\item Compute the absolute z-score:
\code{z = abs(mu / se)}.
\item The one-sided p-value is \verb{1 − Φ(z)}, where Φ is the CDF of the standard normal.
\item The confidence level is \verb{(1 − p-value) × 100 = Φ(z) × 100}.
}
}
\examples{
# Single value
se_sig(mu = 2, vari = 1)

# Vector of values
se_sig(mu = c(-1, 0, 1), vari = c(1, 2, 3))

}
